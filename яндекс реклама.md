# Инструкция по переходу с VK рекламы (MyTarget) на Яндекс рекламу

## Обзор изменений

Данная инструкция описывает процесс замены рекламной сети VK (MyTarget SDK) на Яндекс Mobile Ads SDK в Flutter приложении.

---

## Шаг 1: Обновление зависимостей

### 1.1. Обновление pubspec.yaml

Добавьте зависимость Яндекс Mobile Ads SDK в файл `pubspec.yaml`:

```yaml
dependencies:
  # ... другие зависимости
  # Яндекс реклама
  yandex_mobileads: ^7.16.0
```

Выполните:
```bash
flutter pub get
```

### 1.2. Обновление Android зависимостей

В файле `android/app/build.gradle` удалите зависимость MyTarget SDK:

```gradle
dependencies {
    // Удалите эту строку:
    // implementation 'com.my.target:mytarget-sdk:5.15.0'
    
    // Оставьте только:
    implementation 'com.google.android.gms:play-services-ads-identifier:18.0.1'
}
```

**Важно:** Обновите `compileSdk` до 36 (если требуется плагинами):

```gradle
android {
    compileSdk = 36
    // ...
}
```

---

## Шаг 2: Создание нового сервиса для Яндекс рекламы

### 2.1. Создание файла `lib/services/yandex_ads_service.dart`

Создайте новый сервис для работы с Яндекс рекламой:

```dart
import 'package:flutter/material.dart';
import 'package:flutter/foundation.dart';
import 'package:yandex_mobileads/mobile_ads.dart';

/// Сервис для работы с Яндекс рекламой
class YandexAdsService {
  static final YandexAdsService _instance = YandexAdsService._internal();
  factory YandexAdsService() => _instance;
  YandexAdsService._internal();

  bool _isInitialized = false;
  BannerAd? _bannerAd;
  String? _currentAdUnitId;

  /// Инициализация Яндекс Mobile Ads SDK
  Future<void> initialize() async {
    if (_isInitialized) return;
    try {
      await MobileAds.initialize();
      _isInitialized = true;
      if (kDebugMode) {
        print('Яндекс Mobile Ads SDK инициализирован');
      }
    } catch (e) {
      if (kDebugMode) {
        print('Ошибка инициализации Яндекс Mobile Ads SDK: $e');
      }
      _isInitialized = false;
    }
  }

  /// Создание адаптивного sticky-баннера согласно официальной документации
  /// https://ads.yandex.com/helpcenter/ru/dev/flutter/adaptive-sticky-banner
  Widget buildBannerAd({
    required String adUnitId,
    required BuildContext context,
    double? height,
    EdgeInsets margin = EdgeInsets.zero,
  }) {
    if (!_isInitialized) {
      if (kDebugMode) {
        print('SDK не инициализирован, возвращаем пустой контейнер');
      }
      return Container(
        height: height,
        margin: margin,
      );
    }

    // Создаем баннер если еще не создан или изменился adUnitId
    if (_bannerAd == null || _currentAdUnitId != adUnitId) {
      _bannerAd?.destroy();
      
      // Получаем ширину экрана для адаптивного размера
      final screenWidth = MediaQuery.of(context).size.width.round();
      final adSize = BannerAdSize.sticky(width: screenWidth);
      
      _bannerAd = BannerAd(
        adUnitId: adUnitId,
        adSize: adSize,
        adRequest: const AdRequest(),
        onAdLoaded: () {
          if (kDebugMode) {
            print('Баннер Яндекс рекламы успешно загружен');
          }
        },
        onAdFailedToLoad: (error) {
          if (kDebugMode) {
            print('Ошибка загрузки баннера Яндекс рекламы: $error');
          }
        },
        onAdClicked: () {
          if (kDebugMode) {
            print('Клик по баннеру Яндекс рекламы');
          }
        },
      );
      
      _currentAdUnitId = adUnitId;
      _bannerAd!.loadAd(adRequest: const AdRequest());
    }

    return Container(
      height: height,
      margin: margin,
      child: AdWidget(bannerAd: _bannerAd!),
    );
  }

  /// Освобождение ресурсов рекламы
  void dispose() {
    _bannerAd?.destroy();
    _bannerAd = null;
    _currentAdUnitId = null;
  }
}
```

---

## Шаг 3: Обновление Android кода

### 3.1. Обновление MainActivity.kt

В файле `android/app/src/main/kotlin/ru/davlenie/app/MainActivity.kt` удалите весь код, связанный с MyTarget:

**Удалите:**
- Импорты `com.my.target.ads.MyTargetView`
- Код инициализации MyTarget SDK
- Регистрацию `MyTargetBannerFactory`
- Метод `createBannerAd`
- Метод `loadAd`
- Метод `destroyAd`
- Все обработчики MyTarget канала

**Оставьте только:**
```kotlin

```

### 3.2. Удаление MyTargetBannerFactory.kt

Удалите файл `/MyTargetBannerFactory.kt`

### 3.3. Обновление AndroidManifest.xml

В файле `android/app/src/main/AndroidManifest.xml`:

**Измените комментарий:**
```xml
<!-- Разрешения для Яндекс Mobile Ads SDK -->
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<uses-permission android:name="com.google.android.gms.permission.AD_ID"/>
```

**Удалите:**
```xml
<!-- MyTarget Activity -->
<activity android:name="com.my.target.common.MyTargetActivity" android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"/>
```

---

## Шаг 4: Обновление использования рекламы в Dart коде

### 4.1. Обновление main.dart

В файле `lib/main.dart` замените:

**Было:**
```dart
import 'package:************/services/mytarget_ads_service.dart';

void main() async {
  WidgetsFlutterBinding.ensureInitialized();
  await MyTargetAdsService().initialize();
  // ...
}
```

**Стало:**
```dart
import 'package:*****************/services/yandex_ads_service.dart';

void main() async {
  WidgetsFlutterBinding.ensureInitialized();
  // Инициализируем Яндекс Mobile Ads SDK
  await YandexAdsService().initialize();
  // ...
}
```

### 4.2. Обновление home_screen.dart

В файле `lib/screens/home_screen.dart`:

**1. Измените импорт:**
```dart
import '../services/yandex_ads_service.dart';
```

**2. Измените переменные:**
```dart
// Переменные для рекламы
static const String BANNER_AD_UNIT_ID = 'айди банера'; // ID блока Яндекс рекламы
late final YandexAdsService _adsService;
bool _isAdInitialized = false;
```

**3. Обновите метод инициализации:**
```dart
Future<void> _initAds() async {
  try {
    _adsService = YandexAdsService();
    await Future.delayed(const Duration(milliseconds: 2000));
    await _adsService.initialize();
    if (mounted) {
      setState(() {
        _isAdInitialized = true;
      });
    }
  } catch (e) {
    if (kDebugMode) {
      print('Ошибка инициализации рекламы: $e');
    }
  }
}
```

**4. Обновите метод создания баннера:**
```dart
Widget _buildAdBanner() {
  return _adsService.buildBannerAd(
    adUnitId: BANNER_AD_UNIT_ID,
    context: context,
    height: 60.0,
    margin: const EdgeInsets.only(top: 8.0, bottom: 8.0),
  );
}
```

**5. Обновите метод dispose:**
```dart
@override
void dispose() {
  // Яндекс Mobile Ads SDK автоматически управляет жизненным циклом рекламы
  super.dispose();
}
```

### 4.3. Удаление старого сервиса

Удалите файл `lib/services/mytarget_ads_service.dart`

---


## Особенности Яндекс рекламы

### Адаптивный sticky-баннер

Используется формат **адаптивного sticky-баннера**, который:
- Автоматически подстраивается под ширину экрана
- Высота не превышает 15% высоты экрана
- Не перекрывает основной контент приложения
- Автоматически обновляется

### Документация

Официальная документация по интеграции:
- [Быстрый старт Flutter](https://ads.yandex.com/helpcenter/ru/dev/flutter/quick-start)
- [Адаптивный sticky-баннер](https://ads.yandex.com/helpcenter/ru/dev/flutter/adaptive-sticky-banner)

### Демоблоки для тестирования

Для тестирования можно использовать демонстрационный ID:
```
demo-banner-yandex
```

**Важно:** Перед публикацией замените демо ID на реальный ID блока из панели управления Яндекс рекламы.

---

## Чеклист завершения миграции

- [ ] Добавлена зависимость `yandex_mobileads` в `pubspec.yaml`
- [ ] Удалена зависимость MyTarget из `build.gradle`
- [ ] Создан файл `lib/services/yandex_ads_service.dart`
- [ ] Обновлен `MainActivity.kt` (удален код MyTarget)
- [ ] Удален файл `MyTargetBannerFactory.kt`
- [ ] Обновлен `AndroidManifest.xml`
- [ ] Обновлен `main.dart` (инициализация Яндекс SDK)
- [ ] Обновлен `home_screen.dart` (использование нового сервиса)
- [ ] Удален файл `lib/services/mytarget_ads_service.dart`
- [ ] Исправлен `CardTheme` в `app_theme.dart`
- [ ] Обновлена версия приложения во всех файлах
- [ ] Протестировано в режиме отладки
- [ ] Собран релизный AAB файл
- [ ] Демо ID заменен на реальный ID блока

---

## Шаг 5: Подключение Яндекс AppMetrica (Аналитика)

### 5.1. Создание приложения в AppMetrica

1. Перейдите на https://appmetrica.yandex.ru/
2. Нажмите **"Добавить приложение"**
3. Введите название приложения
4. Выберите платформу: **Android** или **iOS**
5. Укажите Package Name (например: `com.example.your_app`)
6. Скопируйте **API ключ** (формат: `xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx`)

### 5.2. Добавление зависимости

Добавьте в `pubspec.yaml`:

```yaml
dependencies:
  # ... другие зависимости
  yandex_mobileads: ^7.16.0 # Яндекс реклама
  appmetrica_plugin: ^3.3.0 # Яндекс AppMetrica для аналитики
```

Выполните:
```bash
flutter pub get
```

### 5.3. Создание сервиса AppMetrica

Создайте файл `lib/services/appmetrica_service.dart`:

```dart
import 'dart:io';
import 'package:appmetrica_plugin/appmetrica_plugin.dart';
import 'package:flutter/foundation.dart';

/// Сервис для работы с Яндекс AppMetrica
/// Документация: https://appmetrica.yandex.ru/docs/ru/sdk/flutter/analytics/quick-start
class AppMetricaService {
  // API ключи AppMetrica (получить в консоли AppMetrica)
  static const String _apiKeyAndroid = 'YOUR_ANDROID_API_KEY'; // Замените!
  static const String _apiKeyIOS = 'YOUR_IOS_API_KEY'; // Замените!

  static bool _isInitialized = false;

  /// Инициализация AppMetrica
  static Future<void> initialize() async {
    if (_isInitialized) {
      if (kDebugMode) print('[AppMetrica] ⚠️ Уже инициализирован');
      return;
    }

    try {
      // Выбираем ключ в зависимости от платформы
      final apiKey = Platform.isAndroid ? _apiKeyAndroid : _apiKeyIOS;

      // Конфигурация AppMetrica
      await AppMetrica.activate(AppMetricaConfig(
        apiKey,
        logs: kDebugMode, // Логи только в debug режиме
        locationTracking: false, // Отключаем геолокацию для приватности
        sessionTimeout: 30, // Таймаут сессии 30 секунд
        crashReporting: true, // Включаем отправку крэшей
        appVersion: '1.0.0', // Укажите версию вашего приложения
      ));

      _isInitialized = true;
      if (kDebugMode) print('[AppMetrica] ✅ Успешно инициализирован');
    } catch (e) {
      if (kDebugMode) print('[AppMetrica] ❌ Ошибка инициализации: $e');
    }
  }

  /// Отправка пользовательского события
  /// Примеры: 'button_clicked', 'screen_HomeScreen', 'purchase_premium'
  static Future<void> reportEvent(String eventName) async {
    if (!_isInitialized) return;

    try {
      await AppMetrica.reportEvent(eventName);
      if (kDebugMode) print('[AppMetrica] 📊 Событие: $eventName');
    } catch (e) {
      if (kDebugMode) print('[AppMetrica] ❌ Ошибка отправки события: $e');
    }
  }

  /// Отправка события просмотра экрана
  static Future<void> reportScreenView(String screenName) async {
    await reportEvent('screen_$screenName');
  }
}
```

**⚠️ ВАЖНО:** API `appmetrica_plugin` версии 3.3.0 **не поддерживает** параметр `attributes` в методе `reportEvent()`. Параметры нужно передавать в названии события:

**Примеры:**
- `water_added_250ml` вместо `reportEvent('water_added', {amount: 250})`
- `goal_reached_105percent` вместо `reportEvent('goal_reached', {percent: 105})`
- `notifications_enabled_2h` вместо `reportEvent('notifications', {enabled: true, hours: 2})`

### 5.4. Инициализация в main.dart

Обновите `lib/main.dart`:

```dart
import 'services/yandex_ads_service.dart';
import 'services/appmetrica_service.dart';

void main() async {
  WidgetsFlutterBinding.ensureInitialized();
  
  // Инициализируем Яндекс Mobile Ads SDK
  await YandexAdsService().initialize();
  
  // Инициализируем Яндекс AppMetrica для аналитики
  await AppMetricaService.initialize();
  
  runApp(const MyApp());
}
```

### 5.5. Примеры использования

#### Отслеживание навигации между экранами

В `lib/screens/main_navigation_screen.dart`:

```dart
import '../services/appmetrica_service.dart';

class _MainNavigationScreenState extends State<MainNavigationScreen> {
  int _currentIndex = 0;

  // Названия экранов для AppMetrica
  final List<String> _screenNames = [
    'HomeScreen',
    'StatsScreen',
    'SettingsScreen',
  ];

  @override
  void initState() {
    super.initState();
    // Отслеживаем первый экран при запуске
    WidgetsBinding.instance.addPostFrameCallback((_) {
      AppMetricaService.reportScreenView(_screenNames[0]);
    });
  }

  void _onTabTapped(int index) {
    setState(() => _currentIndex = index);
    // Отслеживаем переключение экранов
    AppMetricaService.reportScreenView(_screenNames[index]);
  }
}
```

#### Отслеживание пользовательских действий

```dart
// Кнопка была нажата
await AppMetricaService.reportEvent('button_add_water_250ml');

// Покупка совершена
await AppMetricaService.reportEvent('purchase_premium');

// Настройка изменена
await AppMetricaService.reportEvent('settings_notifications_enabled');

// Уведомления включены с интервалом 2 часа
await AppMetricaService.reportEvent('notifications_enabled_2h');

// Цель достигнута (105%)
await AppMetricaService.reportEvent('goal_reached_105percent');
```

### 5.6. Проверка работы AppMetrica

#### Шаг 1: Соберите и установите приложение

```bash
flutter build apk --debug
adb install build/app/outputs/flutter-apk/app-debug.apk
```

#### Шаг 2: Запустите приложение на устройстве

- Откройте приложение
- Выполните несколько действий (переключите экраны, нажмите кнопки)
- Подождите 1-2 минуты

#### Шаг 3: Проверьте логи (если устройство по USB)

```bash
adb logcat | grep -i AppMetrica
```

Должны появиться строки:
```
[AppMetrica] ✅ Успешно инициализирован
[AppMetrica] 📊 Событие: screen_HomeScreen
[AppMetrica] 📊 Событие: button_clicked
```

#### Шаг 4: Проверьте в консоли AppMetrica

1. Откройте https://appmetrica.yandex.ru/
2. Выберите ваше приложение
3. Перейдите в **"Отчеты" → "События"**
4. Данные появятся через **1-3 минуты** после отправки

**Важно:** Пока приложение не запустится хотя бы раз на реальном устройстве, консоль AppMetrica будет показывать **"SDK не настроен"**. После первого запуска это сообщение исчезнет.

### 5.7. Типичные события для отслеживания

**Навигация:**
- `screen_HomeScreen`, `screen_SettingsScreen`, `screen_ProfileScreen`

**Пользовательские действия:**
- `button_clicked_name`, `form_submitted`, `search_performed`

**Бизнес-события:**
- `purchase_product_id`, `subscription_started`, `trial_activated`

**Настройки:**
- `settings_notifications_enabled`, `settings_theme_dark`, `language_changed_ru`

**Достижения:**
- `achievement_unlocked_first_level`, `goal_reached_100percent`

### 5.8. Просмотр метрик в AppMetrica

После интеграции вы сможете видеть:

**В разделе "Обзор":**
- Установки приложения
- Активные пользователи (DAU/MAU)
- Конверсия установка → первое действие

**В разделе "Отчеты → События":**
- Все пользовательские события
- Количество событий на пользователя
- Популярность различных функций

**В разделе "Список профилей":**
- Детальная информация о каждом пользователе
- История действий пользователя
- Устройства и версии ОС

---

## Чеклист завершения интеграции

### Яндекс Mobile Ads (Реклама)
- [ ] Добавлена зависимость `yandex_mobileads` в `pubspec.yaml`
- [ ] Создан `lib/services/yandex_ads_service.dart`
- [ ] Получен ID рекламного блока в Яндекс Рекламной Сети
- [ ] Инициализация в `main.dart`
- [ ] Баннер интегрирован в UI
- [ ] Реклама не перекрывает контент
- [ ] Протестирована на реальном устройстве

### Яндекс AppMetrica (Аналитика)
- [ ] Создано приложение в AppMetrica консоли
- [ ] Скопирован API ключ
- [ ] Добавлена зависимость `appmetrica_plugin` в `pubspec.yaml`
- [ ] Создан `lib/services/appmetrica_service.dart`
- [ ] API ключ добавлен в сервис
- [ ] Инициализация в `main.dart`
- [ ] Добавлено отслеживание экранов
- [ ] Добавлено отслеживание ключевых событий
- [ ] Приложение запущено на реальном устройстве
- [ ] События видны в консоли AppMetrica
- [ ] Версия приложения обновлена

### Финальные проверки
- [ ] Удалены старые зависимости (MyTarget и др.)
- [ ] Удален старый код рекламы
- [ ] Обновлен `AndroidManifest.xml`
- [ ] Протестировано в debug режиме
- [ ] Собран release AAB/APK
- [ ] Проверены все функции приложения

---

## Полезные ссылки

### Документация Яндекс
- [Яндекс Mobile Ads - Flutter](https://ads.yandex.com/helpcenter/ru/dev/flutter/quick-start)
- [Яндекс AppMetrica - Flutter](https://appmetrica.yandex.ru/docs/ru/sdk/flutter/analytics/quick-start)
- [Рекламная Сеть Яндекса](https://partner.yandex.ru/)
- [Консоль AppMetrica](https://appmetrica.yandex.ru/)

### Поддержка
- [Форум разработчиков Яндекс](https://yandex.ru/support/)
- [GitHub: yandex_mobileads](https://github.com/yandex/yandex-ads-sdk-flutter)
- [GitHub: appmetrica_plugin](https://github.com/appmetrica/appmetrica-flutter-plugin)

---

**Версия инструкции:** 1.0  
**Дата обновления:** 02.11.2025  
**Автор:** betment76

